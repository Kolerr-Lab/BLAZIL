---
# Ansible Galaxy requirements for Blazil cluster provisioning
# Install with: ansible-galaxy install -r requirements.yml

roles: []

collections:
  - name: community.general
    version: ">=8.0.0"
  - name: community.docker
    version: ">=3.4.0"
  - name: ansible.posix
    version: ">=1.5.0"
