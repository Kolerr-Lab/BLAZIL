apiVersion: apps/v1
kind: Deployment
metadata:
  name: blazil-banking
  namespace: blazil
  labels:
    app.kubernetes.io/name: blazil-banking
    app.kubernetes.io/component: banking
spec:
  replicas: 3
  selector:
    matchLabels:
      app: blazil-banking
  template:
    metadata:
      labels:
        app: blazil-banking
        app.kubernetes.io/name: blazil-banking
        app.kubernetes.io/component: banking
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9092"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: blazil-banking
                topologyKey: kubernetes.io/hostname
      containers:
        - name: banking
          image: ghcr.io/kolerr-lab/blazil-banking:latest
          env:
            - name: BLAZIL_ENGINE_ADDR
              value: "blazil-engine:7878"
            - name: BANKING_METRICS_ADDR
              value: "0.0.0.0:9092"
            - name: BLAZIL_AUTH_REQUIRED
              valueFrom:
                configMapKeyRef:
                  name: blazil-cluster
                  key: BLAZIL_AUTH_REQUIRED
          ports:
            - name: grpc
              containerPort: 50052
              protocol: TCP
            - name: metrics
              containerPort: 9092
              protocol: TCP
          resources:
            requests:
              cpu: "250m"
              memory: "128Mi"
            limits:
              cpu: "1"
              memory: "512Mi"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          readinessProbe:
            grpc:
              port: 50052
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            grpc:
              port: 50052
            initialDelaySeconds: 15
            periodSeconds: 10
