apiVersion: apps/v1
kind: Deployment
metadata:
  name: blazil-crypto
  namespace: blazil
  labels:
    app.kubernetes.io/name: blazil-crypto
    app.kubernetes.io/component: crypto
spec:
  replicas: 3
  selector:
    matchLabels:
      app: blazil-crypto
  template:
    metadata:
      labels:
        app: blazil-crypto
        app.kubernetes.io/name: blazil-crypto
        app.kubernetes.io/component: crypto
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9094"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: blazil-crypto
                topologyKey: kubernetes.io/hostname
      containers:
        - name: crypto
          image: ghcr.io/kolerr-lab/blazil-crypto:latest
          env:
            - name: BLAZIL_ENGINE_ADDR
              value: "blazil-engine:7878"
            - name: METRICS_ADDR
              value: "0.0.0.0:9094"
            - name: BLAZIL_AUTH_REQUIRED
              valueFrom:
                configMapKeyRef:
                  name: blazil-cluster
                  key: BLAZIL_AUTH_REQUIRED
          ports:
            - name: grpc
              containerPort: 50054
              protocol: TCP
            - name: metrics
              containerPort: 9094
              protocol: TCP
          resources:
            requests:
              cpu: "250m"
              memory: "128Mi"
            limits:
              cpu: "1"
              memory: "512Mi"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          readinessProbe:
            grpc:
              port: 50054
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            grpc:
              port: 50054
            initialDelaySeconds: 15
            periodSeconds: 10
