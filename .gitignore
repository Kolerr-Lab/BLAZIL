# Rust
target/
**/*.rs.bk
# Cargo.lock is intentionally committed for reproducible CI builds

# Go
vendor/
*.test
*.out
coverage.txt
coverage.html
dist/
bin/

# Go service binaries (compiled in-place)
services/compliance/compliance
services/gateway/gateway
tools/loadgen/loadgen

# Infrastructure
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Build artifacts
dist/
build/

# Node (for any tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
.dockerenv

# Kubernetes secrets
*-secret.yml
*-secret.yaml

# Certificates
*.pem
*.key
*.crt
*.cert
*.pfx
*.p12

# Benchmark results (keep criterion html reports but not raw data)
target/criterion/**/*.csv

# Pre-built stresstest binaries (cross-compiled locally, not committed)
tools/stresstest/stresstest-linux
tools/stresstest/stresstest-linux-arm64
